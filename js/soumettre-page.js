import { fetchCategories, renderSpecificFields, submitOffer } from "./offres.js";import { $, showToast, setLoading } from "./ui.js";import "./main.js";async function init(){const cats=await fetchCategories();$("#categorie").insertAdjacentHTML("beforeend",cats.map(c=>`<option value="${c.id}" data-slug="${c.slug}">${c.nom}</option>`).join(""));$("#categorie").addEventListener("change",e=>renderSpecificFields(e.target.selectedOptions[0]?.dataset.slug,$("#specific-fields")));$("#offer-form").addEventListener("submit",async e=>{e.preventDefault();const form=e.currentTarget;const button=form.querySelector("button[type=submit]");if(!$("#lien_candidature").value&&!$("#email_contact").value){showToast("Ajoute un lien ou un email de candidature.","error");return}const fd=new FormData(form);const details={};$("#specific-fields").querySelectorAll("input,select").forEach(el=>{if(el.value)details[el.name]=el.value});const payload=Object.fromEntries(fd.entries());delete payload.categorie_id;payload.categorie_id=fd.get("categorie_id");payload.details=details;setLoading(button,true,"Envoi");try{await submitOffer(payload);form.reset();$("#specific-fields").innerHTML="";$("#form-message").innerHTML="<div class=notice-success>Merci, votre offre sera examinée par notre équipe avant publication.</div>";showToast("Offre envoyée avec succès")}catch(err){showToast(err.message,"error")}finally{setLoading(button,false,"Soumettre l’offre")}})}init().catch(()=>showToast("Configure Supabase pour activer les soumissions.","error"));