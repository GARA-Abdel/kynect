import { fetchApprovedOffers } from "./offres.js";
export const skills=["Informatique","Communication","Gestion","Langues","Marketing","Design","Santé","Éducation","Droit","Ingénierie","Finance","Entrepreneuriat"];
export function scoreOffer(offer,preferences){let score=0;const reasons=[];const text=`${offer.titre||""} ${offer.description||""} ${JSON.stringify(offer.details||{})}`.toLowerCase();if(preferences.categories.includes(offer.categories?.slug)){score+=3;reasons.push("Catégorie recherchée")}const loc=(offer.localisation||"").toLowerCase();if(preferences.mode==="distance"&&/(distance|ligne|remote|télétravail)/.test(text)){score+=2;reasons.push("Compatible à distance")}else if(preferences.localisation&&loc.includes(preferences.localisation.toLowerCase())){score+=2;reasons.push("Localisation correspondante")}preferences.skills.filter(Boolean).forEach(skill=>{if(text.includes(skill.toLowerCase())){score+=2;reasons.push(`Compétence : ${skill}`)}});if(preferences.level&&text.includes(preferences.level.toLowerCase())){score+=1;reasons.push("Niveau d’études correspondant")}return {score:Math.min(100,Math.round(score/Math.max(1,3+2+preferences.skills.length*2+1)*100)),reasons:[...new Set(reasons)]}}
export async function matchOffers(preferences){const offers=await fetchApprovedOffers();return offers.map(offer=>({...offer,match:scoreOffer(offer,preferences)})).sort((a,b)=>b.match.score-a.match.score)}
